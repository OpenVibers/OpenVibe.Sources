name: ci
on:
  push:
    branches: [main]
  pull_request:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.22.1   # production runs Node 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: npm test
      # Add once the sources service manifest and the sources.* capabilities ship in
      # openvibe-contracts (proposals in docs/capabilities-proposal/ and docs/service-manifest-proposal.json):
      # - run: npx openvibe-contracts-check --service sources --src server
